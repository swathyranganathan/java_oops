package Interface;

public interface Engine {
    int price=78000;   //static final
    void start();
    void stop();
    abstract void acc();
    // all the methods are abstract , because interface allow only interface

}
